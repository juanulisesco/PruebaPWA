[phases.setup]
# Extensiones extra de PHP necesarias para web-push (VAPID) e íconos
aptPkgs = ["libgmp-dev", "libpng-dev"]

[phases.install]
cmds = ["composer install --no-dev --no-interaction --optimize-autoloader"]

[start]
cmd = "php -d extension=pdo_mysql -S 0.0.0.0:$PORT router.php"
